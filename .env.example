# Copy to .env and fill in. Never commit .env.
# All values shown are defaults; uncomment + override only what you need.

# --- LLM (Stage 4 only; Step 1 does not use this) -----------------------------
# GEMINI_API_KEY=
# REVEL_LLM_PROVIDER=gemini
# REVEL_LLM_MODEL=gemini-2.0-flash

# --- Pipeline runtime --------------------------------------------------------
# REVEL_INPUT_PATH=input/restaurants.csv
# REVEL_OUTPUT_DIR=output
# REVEL_DATA_DIR=data
# REVEL_DUCKDB_PATH=data/revel.duckdb
# REVEL_LOG_LEVEL=INFO
